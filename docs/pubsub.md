## History

What we did so far, in order (logic over detail):

1. **Module scaffold** — `infra/modules/pubsub/` with placeholder `main.tf` (Phase 2). Folder and `variables.tf` only; not wired in root `main.tf`; no Pub/Sub resources or apply yet.

## Runbook

(commands for this module go here as work progresses)
